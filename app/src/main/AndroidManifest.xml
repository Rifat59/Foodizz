<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodizz">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/foodizz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/foodizz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResLocationActivity"></activity>
        <activity android:name=".AddItemActivity" />
        <activity android:name=".AddResActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CustomerInfoActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".MughalsDineActivity" />
        <activity android:name=".CityInnActivity" />
        <activity android:name=".TigerGardenActivity" />
        <activity android:name=".MegaActivity" />
        <activity android:name=".RajKacuriActivity" />
        <activity android:name=".BejerdangaActivity" />
        <activity android:name=".KacciGhorActivity" />
        <activity android:name=".JujubaActivity" />
        <activity android:name=".TopInTownActivity" />
        <activity android:name=".RoversCafeActivity" />
        <activity android:name=".ChineseActivity" />
        <activity android:name=".BiriyaniActivity" />
        <activity android:name=".RestaurentTypeActivity" />
        <activity android:name=".RestaurentListActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".LocationActivity"
            android:label="@string/title_activity_location" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LocationActivity2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>